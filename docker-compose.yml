version: "3.9"

services:
  root:
        container_name: recommemder
        build: root
        restart: on-failure
        ports:
          - 5000:5000
        volumes:
          - "./data:/data"
          - "./logs:/logs"
